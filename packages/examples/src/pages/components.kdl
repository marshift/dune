import "../components/say.kdl" // Provides "say"
import "../components/upside-down.kdl" // Provides "upside-down", a styled component

component "nesting-demo" {
	use "say" word="nested"
	p "...That's what components can be!"
}

component "slot-demo" {
	p "There's room for content below me."
	slot "content"
}

page {
	body {
		use "say" word="woah"
		use "upside-down"
		use "nesting-demo"
		use "slot-demo" {
			insert "content" {
				p "Hi, I'm that content!"
			}
		}
	}
}
